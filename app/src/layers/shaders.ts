/**
 * Barrel re-export -- the shader sources have been split into
 * per-shader modules under ./shaders/.
 */
export { DEFAULT_VERTEX, FILTER_VERTEX, getFragmentSource, SHADER_FRAGMENTS } from './shaders/index';
